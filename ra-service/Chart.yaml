apiVersion: v2
name: retail-analytics
description: Retail Analytics service
type: application

# This is the chart version.
# This version number should be incremented each time you make changes to the chart and its templates.
version: "1.0"

# This is the version number of the application being deployed.
# This version number is generated by the packaging process.
# appVersion: "N/A"

dependencies:
  - name: customer-insights
    repository: "file://charts/customer-insights"
    condition: global.services.customerInsights.enabled
  - name: price-sensitivity
    repository: "file://charts/price-sensitivity"
    condition: global.services.priceSensitivity.enabled
